# Supporting Technical Contracts

These documents preserve current technical invariants used by the Phase B
provisioning/materialization implementation. They are supporting authority
only: they do not open a Phase, authorize a Package, authorize deployment or
authorize runtime mutation.

For current execution authority, read
[`../CURRENT_STATE.yml`](../CURRENT_STATE.yml).
